autodiscovery:
  crawlers:
    github/action:
      credentials:
        github.com:
          kind: github
          token: '{{ requiredEnv "UPDATECLI_GITHUB_TOKEN" }}'
      spec:
        digest: false
