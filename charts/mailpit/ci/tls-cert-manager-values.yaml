mailpit:
  smtp:
    tls:
      enabled: true
      method: cert-manager
      certManager:
        dnsNames:
          - smtp.example.com
        issuerRef:
          group: cert-manager.io
          kind: ClusterIssuer
          name: selfsigned
